#include <stdio.h>
#include <string.h>
#include<stdlib.h>
int main()
{
    // intro
    // typedef struct pokemon
    // {
    //     char name[10];
    //     int hp;
    //     int speed;
    // } pokemon;
    // pokemon a[2];
    // strcpy(a[0].name, "Pikachu");
    // a[0].hp = 0;
    // a[0].speed = 1;
    // strcpy(a[1].name, "Charizard");
    // a[1].hp = 9;
    // a[1].speed = 110;
    // for (int i = 0; i < 2; i++)
    // {
    //     printf("Name - %s\n", a[i].name);
    //     printf("Speed - %d\n", a[i].speed);
    // }

    /*Q1. A record contains name of cricketer, his age, number
    of test matches that he has played and the average runs that
    he has scored in each test match. Create an array of structure
    to hold records of 3 such cricketer and then write g program to
    read these records*/
    // typedef struct cricketer{
    //     char name[15];
    //     int age;
    //     int matches;
    //     float avgRuns;
    // }cricketer;
    // cricketer a[3];
    // for(int i=0; i<3; i++){
    //     printf("Name :");
    //     scanf("%s",a[i].name);
    //     printf("Age :");
    //     scanf("%d",&a[i].age);
    //     printf("matches :");
    //     scanf("%d",&a[i].matches);
    //     printf("Average runs :");
    //     scanf("%f",&a[i].avgRuns);
    // }
    // for(int i=0; i<3; i++){
    //     printf("Age :%d\n",a[i].age);
    //     printf("matches : %d\n",a[i].matches);
    //     printf("Average runs : %f\n",a[i].avgRuns);
    // }

    /*:Q2. A record contains name of cricketer, his age, number
    of test matches that he has played and the average runs that
    he has scored in each test match. Create an array of structure
    to hold records of 20 such cricketer and then write g program to
    read these records*/
    // typedef struct date{
    //     int day,month,year;
    // }date;
    // date a,b;
    // a.day = 1;
    // a.month = 1;
    // a.year = 2000;
    // b.day = 25;
    // b.month = 12;
    // b.year = 2000;
    // // scanf("%d %d %d",&a.day,&a.month,&a.year);
    // // scanf("%d %d %d",&b.day,&b.month,&b.year);
    // printf("Date 1(%d/%d/%d)\n",a.day,a.month,a.year);
    // printf("Date 2(%d/%d/%d)",b.day,b.month,b.year);
    // if(a.day==b.day && a.month==b.month && a.year==b.year){
    //     printf("\nBoth dates are Equal");
    // }
    // else{
    //     printf("\nDates are different");
    // }

    
    return 0;
}